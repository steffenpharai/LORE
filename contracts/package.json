{
  "name": "@lore/contracts",
  "version": "0.1.0",
  "description": "Solidity smart contracts for LORE MACHINE",
  "scripts": {
    "compile": "forge build",
    "test": "forge test",
    "deploy:sepolia": "forge script script/Deploy.s.sol:DeployScript --rpc-url $ALCHEMY_BASE_RPC --broadcast --verify",
    "deploy:mainnet": "forge script script/Deploy.s.sol:DeployScript --rpc-url $ALCHEMY_BASE_RPC --broadcast --verify"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}

